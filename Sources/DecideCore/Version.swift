extension Decide {
    /// The version `--version` prints and `--help` shows. It is the tag of
    /// the release that holds this source. DEVELOPMENT.md's Release section
    /// says to set it before tagging.
    public static let version = "0.1.2"
}
